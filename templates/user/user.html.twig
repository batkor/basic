{#
/**
 * @see core/modules/user/templates/user.html.twig
 */
#}

<article{{ attributes.addClass('user', 'user__' ~ view_mode|clean_class) }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
